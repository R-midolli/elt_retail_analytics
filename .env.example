# ============================================================
# ELT Retail Analytics — Environment Variables (EXAMPLE)
# Copy to .env and fill with your local values.
#
# IMPORTANT: Never commit the real .env (it's in .gitignore).
# ============================================================

# ------------------------
# Postgres (Docker)
# ------------------------
POSTGRES_DB=retail
POSTGRES_USER=retail_user
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_PORT=55432

# (Optional) host used by clients (dbt / Power BI)
POSTGRES_HOST=127.0.0.1

# ------------------------
# Raw input file paths (if your extract script uses them)
# ------------------------
RAW_XLSX_PATH=data/raw/online_retail_II.xlsx
PROCESSED_CSV_PATH=data/processed/sales_france_2026.csv

# ------------------------
# dbt (optional overrides)
# If your profiles.yml reads env vars, keep these aligned.
# ------------------------
DBT_TARGET=dev
DBT_SCHEMA=analytics

# Common aliases (keep if profiles.yml expects them)
DB_HOST=127.0.0.1
DB_PORT=55432
DB_NAME=retail
DB_USER=retail_user
DB_PASSWORD=CHANGE_ME
